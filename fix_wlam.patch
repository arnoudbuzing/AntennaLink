--- src/nec2link.c
+++ src/nec2link.c
@@ -21,6 +21,7 @@
 #include "nec2c/nec2c.h"
 
 /* Link to NEC2 global state */
+extern double wlam;
 extern save_t save;
 extern data_t data;
 extern dataj_t dataj;
@@ -103,6 +104,7 @@
     double mhz = MArgument_getReal(Args[0]);
     save.fmhz = mhz;
     data.wlam = TP / (CVEL / mhz);
+    wlam = TP / (CVEL / mhz);
     return LIBRARY_NO_ERROR;
 }
 
